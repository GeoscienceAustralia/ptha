#
# Unique definition of the peak_slip_limit_factor.
# If an event has maximum_slip > peak_slip_limit_factor*scaling_relation_mean_slip,
# then it is assigned a rate of zero
#
peak_slip_limit_factor = 7.5
