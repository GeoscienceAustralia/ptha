# Specify elevation rasters used by the model. 

The raster filenames are in `swals_elevation_files_in_preference_order.txt` - other files were useful in producing or checking it.

The folder [limit_elevation_maxima_polygons](limit_elevation_maxima_polygons) is used to limit the elevation maxima in some regions.
