Namelist filenames including `_B_` correspond to models that cover a smaller area, and are thus more efficient.

The earlier models (without `_B_` in the filename) also work well, and resolve some areas further north. However they proved too computationally expensive for probabilistic runs.
